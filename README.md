# Welcome to My MERN Assignment

Please follow the below steps to validate it:-

- Firstly, you need to initialise the backend server for that direct to /backend directory and use "npm install" and "npm start" in respective order.
- After initiliasing the backend server, Now direct back to the main directory and run the above commands in the same order.
